// Keep the legacy import path aligned with the production authentication screen.
// Credentials are intentionally never embedded or prefilled in the client.
export { default } from './AuthEnhanced';
